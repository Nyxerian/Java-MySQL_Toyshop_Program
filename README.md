# Java-MySQL_Toyshop_Program
Task given was to create a basic database in MySQL and then create a program that would access it for the Toyshop so they could CREATE, UPDATE, SEARCH AND DELETE items from the database with the program.
